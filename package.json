{
  "name": "mlr",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "clean": "rm -rf dist/*",
    "deploy": "npm run clean && npm run build && cd dist && git add --all && git commit -m \"Release at $(date)\" && git push origin gh-pages && cd .."
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^1.0.0-next.11",
    "svelte": "^3.37.0",
    "vite": "^2.6.4"
  },
  "dependencies": {
    "waaclock": "git+https://github.com/taperinstruments/WAAClock.git#process",
    "webmonome": "git://github.com/domchristie/webmonome.git#prepare"
  }
}
